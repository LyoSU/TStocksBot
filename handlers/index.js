const handleProfile = require('./profile')
const handleStock = require('./stock')
const handlePortfolio = require('./portfolio')
const handleChannels = require('./channels')
const handleTop = require('./top')


module.exports = {
  handleProfile,
  handleStock,
  handlePortfolio,
  handleChannels,
  handleTop,
}
